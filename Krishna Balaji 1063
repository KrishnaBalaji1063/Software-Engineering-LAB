#include <stdio.h>
int main() 
{
    int time = 10;
    float a = 0.1;
    float b = 0.5;
    float c = 10;
    float z = a * (time * time) + b * time + c;
    printf("Predicted temperature after %d hours: %f",time,z);
    return 0;
}


output:
Predicted temperature after 10 hours: 25.000000
